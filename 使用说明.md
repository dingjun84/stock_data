# 股票数据获取程序使用说明

## 文件说明

### 主程序
- **run.py** - 全自动模式启动器（推荐使用）
- **daily_all.py** - 拉取当天所有股票数据
- **daily_day.py** - 按天补全历史缺失数据

### 配置文件
- **set_token.py** - 设置 Tushare Token
- **.env** - 存储环境变量（包含 Token）

### 数据文件
- **tushare_stock_basic_20251010230828.csv** - 股票基础信息
- **stock_data/daily/** - 股票日线数据存储目录

## 使用方法

### 方式一：全自动模式（推荐）

```bash
python3 run.py
```

**执行流程：**
1. 自动检查并设置 Tushare Token（如需要）
2. 扫描所有股票，检查数据文件状态
3. 对于没有数据或数据为空的股票，拉取历史数据（到20251010）
4. 自动调用 `daily_day.py` 补全从20251011到今天的数据

**特点：**
- ✅ 完全自动化，无需人工干预
- ✅ 智能跳过已有数据的股票
- ✅ 一次执行完成所有更新
- ✅ 适合首次运行和日常维护

### 方式二：单独使用

#### 1. 拉取当天数据

```bash
python3 daily_all.py
```

- 自动拉取执行脚本当天的所有股票数据
- 适合每日定时任务

#### 2. 补全历史数据

```bash
# 交互模式
python3 daily_day.py

# 自动模式（由 run.py 调用）
python3 daily_day.py --auto
```

**交互模式选项：**
- 模式1：拉取单个交易日
- 模式2：从指定日期开始逐天拉取（需多次执行）

**自动模式：**
- 自动检测数据文件最新日期
- 循环拉取所有缺失日期直到今天
- 无需人工确认

## 数据存储

每只股票的数据单独存储为 CSV 文件：

```
stock_data/daily/stock_data_000001_SZ.csv
stock_data/daily/stock_data_600000_SH.csv
...
```

CSV 文件包含字段：
- ts_code: 股票代码
- trade_date: 交易日期
- open: 开盘价
- high: 最高价
- low: 最低价
- close: 收盘价
- vol: 成交量
- amount: 成交额
等...

## 数据更新策略

### 历史数据（至20251010）
- 使用按股票拉取方式
- 从上市日期开始到20251010
- 支持分批拉取（每批最多6000条）
- 支持增量更新

### 最新数据（从20251011起）
- 使用按日期拉取方式
- 每次拉取一天的所有股票数据
- 自动追加到现有文件
- 支持数据更新和去重

## 注意事项

1. **首次使用**需要设置 Tushare Token
2. **API限流**：程序已内置延时，避免请求过快
3. **非交易日**：周末和节假日没有数据，程序会自动跳过
4. **断点续传**：`daily_day.py` 支持进度保存，中断后可继续
5. **数据完整性**：程序会自动检查并过滤未上市的股票

## 推荐工作流

### 首次使用
```bash
python3 run.py
```
等待完成，将自动拉取所有历史数据并补全到今天

### 日常更新
```bash
python3 run.py
```
或者
```bash
python3 daily_all.py
```
只需几分钟即可完成当天数据更新

### 补全缺失数据
如果中间有几天没有更新，运行：
```bash
python3 run.py
```
或者
```bash
python3 daily_day.py --auto
```

## 常见问题

### Q: Token 在哪里获取？
A: 访问 [Tushare官网](https://tushare.pro/) 注册账号即可获取

### Q: 拉取速度慢怎么办？
A: 这是正常的，为了避免API限流，程序已加入延时

### Q: 可以修改数据截止日期吗？
A: 可以，修改 `run.py` 中的 `end_date = '20251010'`

### Q: 如何只更新部分股票？
A: 直接删除对应的CSV文件，再运行 `python3 run.py`

## 更新日志

### 2025-10-19
- ✅ 实现全自动模式 `run.py`
- ✅ 智能检测缺失股票数据
- ✅ 自动补全历史日期数据
- ✅ 支持断点续传和进度保存
- ✅ 优化用户体验和输出信息

---

如有问题，请检查 Token 配置和网络连接。

